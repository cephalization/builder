import Builder from "./builder";

export const createElements = Builder.createElements;
export const setRoot = Builder.setRoot;
export const registerData = Builder.registerData;
export const deleteData = Builder.deleteData;
